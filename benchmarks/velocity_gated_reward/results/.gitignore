# Raw training/eval artifacts are large and regenerable -- ignored.
# tables/ and plots/ ARE tracked: they are the small, publication-relevant
# outputs of analyze.py and reviewers want them versioned.
runs/
eval/
_archive*/
manifest.json

# Keep the directory placeholder
!.gitkeep
